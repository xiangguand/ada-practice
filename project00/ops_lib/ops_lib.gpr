library project ops_lib is

    for Library_Name use "ops_lib";

    for Source_Dirs use ("src");
    for Library_Dir use "lib";
    for Object_Dir  use "obj";

    package Compiler is
        for Switches ("Ada") use ("-gnata", "-gnato", "-fstack-check");
    end Compiler;

end ops_lib;

